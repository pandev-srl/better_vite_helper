{
  "$schema": "https://context7.com/schema/context7.json",
  "projectTitle": "BetterViteHelper",
  "description": "Rails Engine providing Vite view helpers with automatic dev server detection and manifest-based asset resolution",
  "folders": ["lib", "spec"],
  "excludeFolders": ["spec/dummy"],
  "excludeFiles": [],
  "rules": [
    "Use vite_javascript_include_tag for JavaScript entry points",
    "Use vite_stylesheet_link_tag only in production (returns empty in development)",
    "Configure via BetterViteHelper.configure block in initializer",
    "Run the install generator to copy Vite and PostCSS config files"
  ]
}
