#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"

# Run RSpec - default to spec directory
ARGV.push("spec") if ARGV.empty? || ARGV.all? { |arg| arg.start_with?("-") }
load Gem.bin_path("rspec-core", "rspec")
